# MEMORY.md — Source of Truth

## 프로젝트
**Keystone** — 투자 플랜 관리 SaaS. React 프로토타입(`source/`)을 멀티유저 SaaS(웹+모바일, 추후 월구독)로 전환.
- 레포: `peterkwon248/KeystoneFinal` (public) · 로컬: `Desktop\Keystone Final` (Plot-V3 홈레포 안의 중첩 독립 레포)
- 스펙: `ARCHITECTURE.md`(오케스트레이터) + `DATA_MODEL.md`(DDL) + `API.md`(계약) + `HANDOFF.md`(디자인 히스토리)
- **디자인 기준: `screens/` 스크린샷 그대로 구현** (Linear 스타일 다크 UI). **2026-07-03 새 핸드오프로 6→22장 확장**(07 대시보드~22 종목상세). ✅ **source/core 재조정 = 실측 no-op으로 종결(2026-07-04)**: root `source/` ↔ 새 핸드오프 `source/`는 **라인엔딩(CRLF↔LF) 차이만** — 순수로직 0줄 변경(실제 로직 델타는 `App.jsx`/`P4Views.jsx`의 ResearchBrowser 뷰 2건뿐, 15 리서치 이식 때 흡수). core·골든 무손상, 재생성 불필요. 상세는 NEXT-ACTION 상단

## 확정 방향
- 스택: 모노레포(pnpm+Turborepo) / packages-core / Next.js / Expo / Supabase / 서버 어댑터
- Provider(무료 1순위): KIS·DART·Finnhub·EDGAR·Frankfurter
- **Supabase: 로컬 `supabase start` 기반 먼저 완성 → 클라우드 연결 나중** (2026-07-02)
- 순수 계산 로직은 절대 재작성하지 않고 포팅 + 골든 동치 검증

## 마일스톤 진행 (ARCHITECTURE.md §13)
| # | 내용 | 상태 |
|---|---|---|
| 1 | 모노레포 + core 추출 | ✅ 2026-07-02 (커밋 e1e8967, 골든 89/89) |
| 2 | Supabase 스키마 + Auth | ✅ 2026-07-02 (마이그레이션 4개 + seed, E2E 검증) |
| 3 | 플랜 데이터 DB화 | ✅ 2026-07-02 (롤업 뷰 + 전이 트리거 2종 + 타입 생성, E2E 검증) |
| 4 | 재무 어댑터 (DART/EDGAR) | ✅ 2026-07-02 (apps/server, 14/14 동기화, 실측 교차검증) |
| 5 | 시세 폴링 + FX | ✅ 2026-07-02 (KIS/Finnhub 14/14 + dividend_yield + FX) ← **MVP 데이터 레이어 완료** |
| 7 | 웹 이식 (6보다 선행 결정) | 🔄 2026-07-04(2차 세션) — **원본 6개 스크린(01~06) + 07·16·10·19~22·14 완료**(위 세션). **이번 세션 6기능 추가(커밋 1605d98~bd5456c, push됨)**: **15 리서치**(`research/`+`security-picker.tsx`[공용]+`sec-recents`+`fetchAllSecurities`) · **플랜 리스트 필터**(`plan-filters.ts`+`plans-screen` 필터패널/칩) · **11~13 스크리너**(`screener/`+`screener-ref/data.ts`, 4레이아웃 리스트/보드/히트맵/4분면, core finOf+gradeWithFw) · **18 휴지통**(`trash/` restore/deleteForever) · **Cmd+K 검색모달**(`search/search-modal.tsx`+app-shell Cmd+K 리스너) · **플랜 시나리오 작성모달**(`scenario-author-modal.tsx`+addPlanScenario). **Phase A 뷰 이식 사실상 완료**. 남은 것 = **17 보관함**(S1 스키마)·**B8 플랜생성 위저드**(핵심·큼)·**B5 SecurityPeek**·**B9 adhoc 시나리오**(S2 스키마). 🗺️ 잔여 계획은 NEXT-ACTION + ARCHITECTURE §13. ✅ source/core 재조정 no-op 종결 |
| 6 | 실시간 WS **+ 과거 시세 히스토리 백필** | ✅ **완료(2026-07-05 4차)**: 3차의 OHLCV 백필·seam 교체 위에 **실시간 WS 스트리밍**(KIS/Finnhub WS→live_quotes→Supabase Realtime→UI) + **마지막 mock seam 실데이터화**(gap iv/screener perLo·perHi — "백필 아니라 seam 교체"였음, 재무 5년 이미 DB) 완료. **mock seam 0개 · 앱 실제 오늘 기준 코히런트 · 장중 실시간 시세.** 상세 아래 "4차 세션" |
| 8·9 | 모바일 / 구독 | |

## 커밋/PR 히스토리
- `685525a` 초기 커밋: 프로토타입 + 스펙 + 스크린샷 (2026-07-02)
- `e1e8967` 마일스톤 1: 모노레포 + packages/core + 골든 테스트 (2026-07-02, main 직푸시)
- **2026-07-04(2차 세션) 6기능 이식 (main 직푸시):** `1605d98` 15 리서치+SecurityPicker · `bd33df7` 플랜 리스트 필터 · `354467f` 11~13 스크리너(4레이아웃) · `4d5970f` 18 휴지통 · `e2a5fbc` Cmd+K 검색모달 · `bd5456c` 플랜 시나리오 작성모달
- **2026-07-05 세션 (S1/S2 스키마 + 보관함 + adhoc + 플랜생성 + 규칙자동화 v1):** 마이그레이션 2건(`20260704000100_archive_adhoc`·`20260705000100_rule_flags`) + 아래 "이번 세션" 이음새들. 단일 커밋 `session:` (main 직푸시)
- **2026-07-05 2차 세션 (규칙자동화 완성 + 뷰 write-path + 마일스톤6 착수, 3커밋 push):** `696d2fd` 규칙자동화 스텝4(편집/삭제 CRUD+인라인 작성폼+자동배지)+v2(4전략 새 트리거 time/weight/trailing/path, **전량 웹레이어**) · `e6a41ba` 밸류애버리징 경로 오버레이(옵션2)+B5 SecurityPeek(전체상세 슬라이드오버)+시나리오 편집/삭제 CRUD · `b6c7650` Phase C 스키마 기반(price_history+notifications 마이그레이션+/api/ohlc)+Tiingo 결정. 전부 브라우저 E2E·골든 102·core 무수정
- **2026-07-05 3차 세션 (Phase C 실데이터 전환, 6커밋 push):** `063c8e4` OHLCV 백필 파이프라인(Tiingo/KIS 일봉 어댑터+sync:ohlc CLI+14종목 5년 백필 17,339봉+/api/ohlc max_rows 버그수정) · `8505099` trajectory 시장가 seam(mock seededWalk→실 종가) · `c2b9972` genSpark/mockChange seam(실 스파크·등락률+server-only 회귀수정) · `86c7e69` 날짜앵커 중앙화(단일 clock.ts=KS_REF 파생) · `85b22d7` 규칙발동 워커(rules 평가→notifications insert→인박스) · `43bd2bf` fin-history seam(밴드차트 실 연간종가). 전부 브라우저 E2E(+DB service_role 조회)·골든 102·web/서버 tsc 0·core 무수정
- **2026-07-05 4차 세션 (live 전환 + 백로그 완전소진, 6커밋 push):** `67c6942` 날짜앵커 real-now(frozen 6/26→실제 오늘; refNow 실 now화·trajectory/timeline rolling 10개월 창 재설계로 todayT NaN 해소·`inferYearWeb`가 core inferYear 6/26 규칙 복제 = 골든 무손상) · `d46a251`+`fbef3b5` 실시간 WS 스트리밍(KIS approval_key+H0STCNT0·Finnhub WS→`stream-quotes` worker→`live_quotes`→**Supabase Realtime**→브라우저 라이브; 커스텀 SSE 안 만듦) · `4757879` 마지막 mock seam 실데이터화(gap iv/screener perLo·perHi; **"백필 아니라 seam 교체" — 재무 5년 이미 DB**, EPS=net/(shares×1e6)·PER밴드=연말종가/EPS·IV=EPS×√perLo·perHi) · `f6533f8` 인박스 트리아지 DB 이전(read/resolved/muted localStorage→`inbox_triage` 전용테이블·복합노트키·기기간 동기화) · `ad4a159` adhoc 시나리오 CRUD(updateSecurityScenario/deleteSecurityScenario·플랜 패턴 미러). 전부 브라우저 E2E·골든 102·web/서버 tsc 0·**web 프로덕션 빌드 ✓**·core 무수정. **워크로그 Remaining Tasks 전부 done.**

## 핵심 기술 사실
- `packages/core` 골든 테스트: `pnpm --filter @keystone/core test` / 골든 재생성: `... goldens` (scripts/generate-goldens.mjs가 source/*.jsx를 vm에서 eval)
- **골든 함정 = 카탈로그 배열 deep-equal**: `i18n-reference.test`가 core `RULE_TRIGS`/`RULE_ACTS`/`EXEC_STRATEGIES`를 읽기전용 `source/` 골든과 **deep-equal** → **core에 항목 추가 시 골든 깨지고 재생성 불가**(생성기가 source 재슬라이스). evalRule은 골든 replay되나 trig 집합이 closed 아니라 다른 곳 추가는 무해. **해법 = `closeout.ts` 패턴(core 감싸고 웹레이어 확장)**: 규칙 v2 신규 트리거는 `lib/rule-trigs-v2.ts`(웹 카탈로그+findTrig 병합) + `lib/rule-eval-v2.ts`(evalRuleV2가 기존은 core evalRule 위임·신규는 콕핏 수학 미러). 새 condition은 jsonb라 마이그레이션 불필요
- **웹 확장 타입 패턴**: `UIRule extends Rule`(id/isAuto/cond)·`UIScenario extends Scenario`(dbId/caseT) — core 타입(골든) 무수정, PLAN_SELECT에 id 추가·UIPlan에서 배열 오버라이드. 편집/삭제 타겟팅에 DB id 필수
- i18n 비대칭: `tip_trough_peak` ko 전용 (en 577/ko 578) — 원본 그대로
- i18n 중복 키 dedup(last-wins): `simulator`, `scThesisPh`
- format 모듈은 모듈 상태(KEYSTONE_FX=1380, KEYSTONE_DISP) 보유 — 실앱에서 FX API로 교체 지점
- Supabase CLI = 루트 devDependency(`pnpm supabase ...`, v2.109) — 크로스머신 재현성. `pnpm-workspace.yaml`에 `onlyBuiltDependencies: [esbuild, supabase]`
- **PG17 hardened defaults**: RLS만으론 부족, anon/authenticated에 테이블 GRANT를 마이그레이션으로 명시해야 함 (`20260702000400_grants.sql`)
- 로컬 스택: API 54321 / DB 54322 / Studio 54323 / Mailpit 54324. 스키마 재적용 = `pnpm supabase db reset` (migrations + seed.sql)
- 소프트 인증 = `enable_confirmations=false` (가입 즉시 세션, email_verified는 profiles에서 추적) — config.toml 기본값 유지
- **뷰는 `security_invoker=true` 필수** (기본은 owner 권한 실행 → RLS 우회로 타 유저 데이터 유출) — `plan_positions`에 적용
- 상태 자동전이는 DB 트리거가 정본: `t_exec_activate`(매수→active), `t_exec_close`(전량매도→closing, 매도 전 보유>0 가드). 시나리오 status는 시세 필요 → 서버 워커 몫(마일스톤 6)
- DB 타입: `pnpm db:types` → `packages/core/src/types/database.ts` (스키마 변경 시 재생성)
- 재무 동기화: `pnpm --filter @keystone/server sync:financials` (`--market KR|US`, `--tickers`). 시크릿은 루트 `.env`(DART_API_KEY 등, .env.example 참고) — 클라이언트 노출 금지
- **DART corpCode.xml 함정**: `<list>` 블록 단위로 파싱해야 함 — lazy 정규식은 비상장사(빈 stock_code)의 corp_code를 다음 상장사와 잘못 짝지음
- EDGAR: fp=FY + 10-K/20-F + duration ≥330일 필터 필수 (10-K 안의 Q4 3개월치가 연간값 오염). ADR(TSM)은 ifrs-full 태그 폴백. 연도 라벨은 보고서 fy가 아니라 기간 종료일 기준
- 시세 동기화: `pnpm --filter @keystone/server sync:quotes` — KR=KIS, US=Finnhub → securities.last_close + 최신연도 dividend_yield
- **KIS 토큰은 발급이 분당 1회 제한** → .cache/kis-token.json 디스크 캐시 (24h 유효, 만료 1h 전 갱신). 실전/모의는 KIS_ENV=real|vts
- KIS REST는 연속 호출 시 간헐 500 → 어댑터가 400ms 백오프 재시도로 흡수
- 배당: KR = DART alotMatter 주당현금배당금(보통주) ÷ 현재가, US = Finnhub currentDividendYieldTTM. 무배당(삼바/AMD/TSLA)은 null이 정상
- **`.env` 키 5종 필요** (git 밖!): DART_API_KEY / FINNHUB_API_KEY / KIS_APP_KEY / KIS_APP_SECRET / KIS_ENV=real. 웹은 apps/web/.env.local (각 .env.example 참고)
- apps/web: 프로토타입 CSS 통복사(styles/) + reticle 클래스 그대로 → 픽셀 충실. Lic=lucide-react 동적 매핑. 전략/관점은 core 프리셋(LIBRARY_LOCKED)
- Next dev 함정: 라우트 대량 추가 후 하이드레이션 무반응이면 `.next` 삭제 후 재시작 (상세는 LLM Wiki nextjs-dev)
- 웹 로컬 테스트 계정: webtest@keystone.local / web-test-password-1 (이 머신 로컬 DB 한정 — 타 머신은 새로 가입)
- 플랜 뷰 이식 이음새(재사용): `apps/web/lib/plan-mapper.ts`(DB row→프로토타입 Plan + PLAN_SELECT 쿼리절 + toMonD/toRelToken 날짜변환), `lib/trajectory.ts`(planTrajectory mock 궤적 — 마일스톤 6에서 실데이터로 교체), `components/plan/`(scenario-gauge·sparkline·list/board/timeline-view·display-panel·group). StatusIcon/StrategyBadge는 `components/icons.tsx`로 이동
- **프로토타입 리스트/보드 전략 뱃지 비대칭 그대로 이식**: ListView는 `plan.execId`→EXEC_STRATEGIES(전략), BoardCard는 `plan.strategyId`→STRATEGIES(관점). 원본 버그성 비대칭이나 골든 원칙상 보존
- 로컬 플랜 시드: `node apps/web/scripts/dev-seed-plans.mjs` — webtest 유저 + 프로토타입 11 플랜(전 상태 커버). service_role로 RLS 우회, idempotent(재실행 시 유저 플랜 삭제 후 재생성)
- **tsconfig `allowImportingTsExtensions:true` 필요** (core가 `../reference/index.ts`처럼 .ts 확장자로 import — 소스 배포 방식)
- **`next build`는 dev 서버 끄고 `.next` 삭제 후** 실행 — 동시 접근 시 dynamic route에서 PageNotFoundError(캐시 충돌)
- **앱 셸 레이아웃**: 프로토타입 App.jsx 구조 = `.app`(flex column) > banner + `.app-row`(flex row) > 사이드바 + main. `.app-row` 래퍼 빼먹으면 사이드바+메인이 세로로 쌓임(2026-07-03 버그 수정)
- **사이드바 도구 섹션**: `lib/sidebar-config.ts`(OPTIONAL_DESTS 7종/SB_CFG0/SB_ORDER0/normalizeSidebar/mergedKeys), `components/shell/sidebar-config.tsx`(Context, cfg/order/pinned를 **profiles.sidebar(jsonb)로 서버 동기화**), `customize-modal.tsx`(핀→상단·표시토글·드래그순서·기본값복원). 상단 고정=pinned, 도구 섹션=cfg true & !pinned
- **⚠️ supabase-js 쿼리 빌더는 lazy thenable** — `.then()`/`await` 호출해야 HTTP 요청 발사됨. `void supabase.from().update().eq()`는 빌더만 만들고 **요청 안 감**(profiles.sidebar 영속이 조용히 실패했던 버그). mutation은 반드시 실행할 것
- **04 플랜 상세 이식 이음새(2026-07-03)**: `components/plan/`에 mini-dropdown·scenarios-tab·gap-tab·execution-ledger·perf-band·financials-tab·activity-tab. mock 시계열은 `lib/gap-history.ts`(GapTab priceHistory/ivHistory/scenarioHistory)+`lib/trajectory.ts`. 재무는 `lib/fin-mapper.ts`(DB security_financials 완전하면 우선, 아니면 core FIN_SEED 폴백). 헤더 픽커 영속=`app/(shell)/plans/[id]/actions.ts`. core 승격 골든 92: scAutoStatus/scProbOf/computeLedger/buildFinFromSeed(seed 분리)
- **04 상세 3탭 추가 이식(2026-07-03 밤)**: `components/plan/`에 **strategy-tab·indicators-tab·valuation-tab**. 전략=콕핏 6종 오버레이(isTime/VR/Weight/Grid/Momentum/Price)+룰카드, 투자지표=5뷰모드(카드/게이지/히트맵/표/차트)+프레임워크 렌즈, 밸류에이션=적정가 계산기+민감도+역산+밴드차트2종. mock priceHistory=`lib/fin-history.ts`(밴드차트·스파크라인용, 마일스톤6 교체). 서버액션 추가(actions.ts): setGoal·toggleRule·applyValuationTargets. plan-mapper에 DB rules(condition/action/last_fired)→리치 Rule 디코드 + custom_fields.goal→plan.goal
- **core 승격 골든 92→102(2026-07-03 밤)**: `evalRule`·`ruleWarn`(analytics, 룰 상태/경고) + 룰 카탈로그(RULE_TRIGS/ACTS/LEGACY_DESC/STATE_LABEL·FIELD_TIPS·STRAT_VAL_KO·locStratVal, reference) + Rule 타입 act?/custom?/edited?. 골든 슬라이스(evalRule/ruleWarn) + tests/rules.test.ts(108 배터리). 밸류에이션/투자지표 순수로직(calcValuation·seedFinancials·seedSlots·gradeOf·IND_THRESH·KS_METRIC_DICT/FORMULA)은 이미 core에 있었음(추가 승격 불필요)
- **⚠️ SWC ≠ tsc(2026-07-03 밤, 중요)**: `tsc --noEmit` 통과해도 Next(SWC) 파서는 **JSX 자식/표현식 안의 제네릭 캐스트**(`(x as Record<A,B>)`·`({} as Partial<Foo>)`·IIFE-in-JSX)를 파싱 못함 → `<A>`를 JSX 태그로 오인 → `Expected '</', got jsx text`(엉뚱한 줄 지목), 브라우저는 stale/깨진 렌더. **해결: 제네릭 캐스트를 JSX 밖 statement(const)로 hoist**. 단일 타입 캐스트(`as {v?:number}`)는 OK. typecheck 그린만 믿지 말고 브라우저 콘솔/`preview_logs`로 SWC 컴파일 확인. dev 서버가 stale 에러 물리면 **재시작**(Windows 파일워처 재컴파일 누락)
- **밴드차트 배치 = 밸류에이션 탭 전용**: ValFairBandChart·MultipleBandChart는 프로토타입에서 `valuation_view.jsx`만 호출(IndicatorsTab 미호출 — grep 확정). 투자지표 이식 때 잘못 넣었다가 밸류에이션으로 이전. `fin` prop + `lib/fin-history` mock으로 렌더. 투자지표는 스파크라인 툴팁용으로 fin-history 계속 사용
- **07 대시보드(현황) 이식 이음새(2026-07-04)**: `components/plan/dashboard-view.tsx`(신규) — DashboardView/DashRow/DashStat/PortfolioTreemap + 순수헬퍼(squarifyTreemap·groupedTreemap·tmapGroupOf·dashHeatColor·refTarget). 재사용: Flag/StatusIcon/Lic(icons)·Sparkline·MiniDropdown·EXEC_STRATEGIES/PORTFOLIOS/STATUS_ORDER(core reference)·planReturn/planTag(core analytics)·fmtCompact(core format)·orderPlans(web `./group`, core 아님). 원본 `securityOf(t).sector`→plan에 `sector` 필드 추가로 대체(plan-mapper: UIPlan.sector + PLAN_SELECT securities에 `sector` 컬럼 추가). **openPlan 탭 딥링크 완료**: `plans-screen` openPlan(p, tab?)→`?tab=`, `detail-view` 초기탭을 `useSearchParams().get("tab")`(알려진 탭키 검증, 폴백 scenarios)로. dash-row→체결 탭 딥링크 E2E 검증됨. `tickN/tickDir`(mock 틱플래시)는 core Plan에 없어 생략(다른 뷰도 동일, 시각 동일)
- **16 인사이트(Insights) 이식 이음새(2026-07-04)**: `components/insights/insights-screen.tsx`(신규, 클라이언트) — InsightsView + 7 하위(InsSection·InsGauge·PerfScatter·ScenarioAccuracy·PerformanceSection·ProcessHealth·WinLoss). export `InsightsScreen({plans})`가 `usePrefs()`로 lang→`I18N[lang]`=t, onOpenPlan=router.push. **대시보드와 동일 성격**(기존 plans 파생 순수 뷰, 새 DB 불필요). 라우트=`(shell)/insights/page.tsx`(journal 서버페치 템플릿 복제, static이 `[dest]` placeholder override — DESTS 맵에서 insights 제거). 단일플랜 scope는 기이식 `InsightsTab` 재사용. PerfScatter SVG 좌표계산은 JSX 밖 hoist(SWC 회피). planinsights.jsx는 미사용(플랜상세 탭용). i18n 키 전부 기존재(추가 0)
- **19~22 종목상세(SecurityDetail) 이식 이음새(2026-07-04)**: `lib/security-mapper.ts`(mapSecurity→UISecurity + secPlanOf→합성 UIPlan + SECURITY_SELECT/SECURITY_FIN_SELECT) · `app/(shell)/securities/[ticker]/`(page 서버페치+actions.ts `toggleWatch` 서버액션) · `components/securities/security-detail.tsx`(SecurityChart·SeasonalityHeatmap·SecurityDetailScreen). **핵심 = 재무/투자지표(21)/밸류에이션(22) 탭은 기존 FinancialsTab/IndicatorsTab/ValuationTab을 `plan={secPlan} fin={fin}`으로 재사용**(plans/[id] 패턴 동일, fin=buildPlanFin). **관심토글**: watchlist 테이블(user_id,ticker unique) insert/delete + 낙관적 useState. **⚠️ eps 유도**: securities/security_financials엔 eps 없음 → **같은 ticker 플랜에서 유도**(`plans.find(p=>p.ticker===tk && p.eps>0)?.eps`, updated_at desc라 최신 플랜) → PER/EPS/밸류에이션 실값. 플랜 없는 종목은 eps=0 폴백(seam). **후속 보강 완료(2026-07-04)**: ①**이 종목의 시나리오**(플랜 시나리오 — linked 재사용 집계, 행→`/plans/[dbId]?tab=scenarios` 딥링크) ②**종목 메모**(journal_entries ticker 스코프 CRUD, addSecNote/editSecNote/deleteSecNote, "일지에도 함께 모임"). **여전히 defer**: adhoc 종목단독 시나리오·SecurityPeek 팝오버·Cmd+K SearchModal·onCreatePlan(버튼 no-op)·onAddScenario("+시나리오추가" no-op). **mock seam**: `change` 일일%(DB 미저장 → `mockChange(ticker)` FNV-1a→[-4.5,+5.0] 결정적 혼합부호, 마일스톤6 실데이터)·spark(genSpark). ⚠️ **change는 처음 sparkChange로 전종목 +5.00% saturate 버그**(genSpark가 pts[n-1]=base 강제) → mockChange로 교체. 라우트=`/securities/[ticker]`
- **14 관심종목(Watchlist) 이식 이음새(2026-07-04)**: `components/watchlist/watchlist-screen.tsx`(WatchlistView P4Views.jsx:6-122) + `lib/securities-list.ts`(fetchWatchedSecurities: watchlist inner-join securities→UISecurity[], **공용 — research/screener 재사용**) + `lib/gics.ts`(GICS_SECTORS). NPlansBadge·plansForTicker 이식. FilterPanel(market/sector/plans/change)·DashStat 재사용. dev-seed에 watchlist 8종목. onOpenSecurity→`/securities/[ticker]`. change는 mockChange(위 참조)
- **10 시나리오 모니터 이식 이음새(2026-07-04)**: `components/scenarios/scenarios-screen.tsx`(신규, 클라이언트) — ScenariosScreen wrapper(toolbar filter/display 버튼+panel state)+ScenariosMonitor(P4Views.jsx:920-1020). 전 플랜 시나리오 크로스뷰(헤드라인 추적/근접/돌파/이탈/평균괴리+상태·종목·케이스 그룹+gap%). scAutoStatus(core), 시나리오 상수는 신규 `lib/scenario-ref.ts`(SC_STATUS_COLOR/SCEN_ORDER/SC_CASE_ORDER/SC_CASE_LAB/scCaseLabel). **공용 이식 2건**: `components/plan/filter-panel.tsx`(Panels.jsx:84-149, **watchlist/screener/archive 언블록**) + `components/plan/dash-stat.tsx`(dashboard-view에서 DashStat 추출, 양쪽 import). **defer**: adhoc 종목단독 시나리오(SECURITY_SCENARIOS — 웹 데이터모델 없음, 종목상세 때)·시나리오 작성 모달(onNewScenario/SecurityPicker/ScenarioAuthor — "새 시나리오" 버튼은 렌더 유지 no-op). securityOf→firstRowOf(ticker→첫 plan row에서 name/flag). 라우트 `(shell)/scenarios/`
- **15 리서치 + 공용 SecurityPicker 이식 이음새(2026-07-04 2차)**: `components/securities/security-picker.tsx`(공용 검색+KR/US 드롭다운, 전역 SECURITIES→`securities` prop, marketSegs 본문 hoist로 SWC 회피 — Cmd+K/시나리오모달 재사용) · `components/research/research-screen.tsx`(ResearchBrowser P4Views:874-912, 최근본종목+관심종목) · `app/(shell)/research/page.tsx` · `lib/sec-recents.ts`(getSecRecents/pushSecRecent localStorage, SSR 가드) · **`lib/securities-list.ts`에 `fetchAllSecurities(supabase,userId)` 추가**(전 종목 UISecurity[]+watched, **스크리너/검색 공용**) · `security-detail.tsx`에 마운트 시 pushSecRecent 추가(프로토타입 openSecurityFull 대응). i18n 키 전부 기존재
- **플랜 리스트 필터 이식 이음새(2026-07-04 2차)**: `lib/plan-filters.ts`(matchesFilters/catOfPlan/fwCatOfPlan/filterValueLabel, source/Panels.jsx 순수 이식 — **웹 인라인, core 승격 안 함**: 형제 뷰 watchlist가 필터 술어를 뷰 레이어에 두는 것과 일치) · `plans-screen.tsx`에 필터 state/버튼/cats 5축(상태·포트폴리오·전략[EXEC_CATS 그룹]·시나리오·수익률)/칩/공용 FilterPanel. **⚠️ 버그 잡음**: 포트폴리오 칩이 core PORTFOLIOS 참조라 DB UUID 노출 → **실 portfolios prop으로 이름 해석**(chipValLabel). 범위 밖: Saved Views·gap/dwell(matchesFilters엔 완전 이식)
- **11~13 스크리너 이식 이음새(2026-07-04 2차)**: `lib/screener-ref.ts`(SEC_SEED[eps/perLo/perHi mock]·SCV 상수·finFlags·numCats·scvHeatScore/Change) · `lib/screener-data.ts`(buildScored — core `finOf`(seed/index.ts 지표)+`gradeWithFw`(screener, 렌즈별 채점)+finFlags) · `components/screener/screener-screen.tsx`(4레이아웃 리스트/보드/히트맵/4분면, 8관점 렌즈, verdict 통과/관찰/탈락, 스크리너 전용 필터[지표조건/밸류/시장/섹터/재무조건]). **⚠️ mock seam**: eps·perLo·perHi가 웹 DB에 없어 `SEC_SEED`(securities.jsx 값 그대로)로 채움 — 마일스톤6 교체. **4분면 물리시뮬(60-iter 충돌해소)은 useMemo hoist**(JSX-inline IIFE 금지, SWC)
- **18 휴지통 이식 이음새(2026-07-04 2차)**: `app/(shell)/trash/`(page: plans `.not("deleted_at","is",null)` 페치 + actions.ts) + `components/trash/trash-screen.tsx`. 서버액션 `restorePlan`(deleted_at=null soft) · `deleteForeverPlan`(하드 delete). dbId(uuid)로 뮤테이션. **⚠️ 소프트삭제(deleted_at set) 액션은 아직 없음** → 휴지통은 다른 경로로 삭제된 플랜을 받는 뷰(테스트는 스크립트로 소프트삭제 시드)
- **Cmd+K 전역 검색모달 이식 이음새(2026-07-04 2차)**: `components/search/search-modal.tsx`(SearchModal SecurityView:365-458 — 플랜/종목[마켓토글]/전략/관점 4그룹, 6개 캡+N more, recency/최근조회 정렬, 미니스파크) + **app-shell.tsx에 전역 Cmd/Ctrl+K keydown 리스너**(setSearch 토글)+Escape + 사이드바 검색버튼 배선. **데이터는 모달 마운트 시 클라이언트 페치**(fetchAllSecurities+plans, supabaseBrowser). 네비: /plans/[dbId]·/securities/[ticker]·/strategy/[id]
- **플랜 시나리오 작성모달 이식 이음새(2026-07-04 2차)**: `components/plan/scenario-author-modal.tsx`(케이스[불/베이스/베어]+목표가+근거, 역산 수익률/PER 프리뷰, SecurityPicker 제외 — 플랜 종목 고정) + `plans/[id]/actions.ts`에 `addPlanScenario`(scenarios insert) + scenarios-tab에 "+시나리오추가" 타일. **⚠️ 중요 스키마 사실**: `scenarios` 테이블엔 **unique(plan_id,case_t) 제약 없음**(인덱스만) → **플랜당 케이스별 N개 insert 가능**, `label` jsonb가 커스텀명. case_t는 bull/base/bear enum(NOT NULL)만 만족하면 됨 → 플랜 대상 시나리오 추가는 **S2 스키마 없이 가능**(adhoc 종목단독만 plan_id nullable=S2 필요=B9). status enum default 'pending'
- **⚠️ HMR deps-size 고스트(2026-07-04 2차, 오진 주의)**: 같은 파일을 두 executor가 연속 수정(스크리너 리스트→레이아웃 추가)해 useEffect deps가 4→6개로 늘자, dev 서버 HMR이 옛 버전(4-dep) fiber를 보존 → React 콘솔에 `useEffect deps changed size between renders`(4→6) 반복 경고. **실제 코드는 정상**(정적 6-dep). **fresh 서버 프로세스 재시작으로 소멸** 확인. 같은 파일 연쇄 수정 후 deps-size 경고 뜨면 서버 재시작부터. (기존 "HMR 겹침 아티팩트"의 useEffect 변형)
- **⚠️ fresh 서버 네비게이션 검증 함정(2026-07-04 2차)**: dev 서버 재시작 직후 첫 라우트 방문은 온디맨드 컴파일(3~5초)이라 `router.push` 후 2.5초로는 URL 미변경 → **네비 안 됨 오판**. 6초 대기하면 정상. Cmd+K→종목 클릭 네비 검증 때 발생. 코드 버그 아님
- **⚠️ HMR Fast Refresh 겹침 아티팩트(2026-07-04, 오진 주의)**: 백그라운드 executor가 라우트/컴포넌트 파일 저장 → Next Fast Refresh 재컴파일 중 React가 **stale+new 트리를 잠깐 겹쳐 렌더** → 우측 디테일바(inbox-props/PropsSidebar)가 2개로 보이는 스크린샷 발생(인박스·일지 동시). **실제 버그 아님** — 하드 리로드 후 DOM은 단일(.app/.app-row/.sidebar/.inbox-props 각 1). 코드상 우측바는 1회만 렌더(inbox-screen `{sel && <InboxProps/>}`·detail-view `<PropsSidebar/>`). 백그라운드 이식 도중 화면 겹침 신고 오면 리로드로 재현확인부터
- **⚠️ JS-측정 위젯(트리맵) + 헤드리스 프리뷰 함정(2026-07-04)**: PortfolioTreemap은 ResizeObserver로 `clientWidth` 재서 타일 배치 → **뷰포트 0×0이면 타일 0개**(CSS 레이아웃 뷰인 list/board는 영향 없음). 프리뷰 검증 시 `preview_resize`로 폭 준 뒤 **페이지 리로드**해야 컴포넌트가 올바른 폭으로 마운트(늦은 resize는 RO 재발화 놓칠 수 있음). preview_screenshot 여전히 간헐 타임아웃 → eval로 DOM 구조 검증(.dash-tmap-tile/.dash-tmap-grp 카운트)
- **dev 프리뷰 포트**: 다른 챗이 3000 점유 시 `.claude/launch.json`에 `autoPort:true` + `runtimeArgs`를 `exec next dev`로(package.json dev의 하드코딩 `--port 3000` 우회) → 프리뷰 하네스가 PORT env로 자동 할당
- **⚠️ 실데이터 전환 필수(2026-07-03 확정, NEXT-ACTION 상세)**: ①**과거 시세 히스토리 백필이 마일스톤 6 핵심**(forward-only 축적은 실사용 불가 — 유저 과거 차트/보유기간 전제). KIS 일봉 + US 대안provider 조사 + `security_price_history` 테이블 신규. 교체 지점=trajectory.ts/gap-history.ts. ②**날짜 앵커** KS_REF=2026-06-26·GapTab new Date(2026,5,..) 하드코딩 → 실제 today 교체. ③재무는 `sync:financials`로 DB 채우면 fin-mapper가 코드변경0으로 자동 실측 전환(공시주기 기준)

### 2026-07-05 세션 이음새 (S1/S2 · 보관함 · adhoc · 플랜생성 · 규칙자동화 v1)
- **S1/S2 마이그레이션(`20260704000100_archive_adhoc.sql`)**: `plans.archived_at`(청산과 별개 축 — 방식2 채택, 프로토타입 status=closed 아님) + `scenarios` plan_id nullable·`ticker`·`user_id`·CHECK(plan_id XOR ticker+user_id). **⚠️ RLS 교훈: plan_id nullable화가 기존 조인기반 `p_scenarios` 정책의 소유권 경로를 깨뜨림 → `user_id` 컬럼 + 정책 재정의(plan 소유 OR user_id 본인) 필수.** GRANT는 기존대로.
- **17 보관함**: `components/archive/archive-screen.tsx`(검색·필터[portfolio/strategy/return]·정렬·복구) + `(shell)/archive/`(page: `.not(archived_at is null)` + actions `restorePlanFromArchive`) + `[dest]` DESTS에서 archive 제거(static override). 진입점 = **플랜 상세 헤더 오버플로 메뉴(⋯)** → 보관/휴지통 (`detail-view.tsx` + `plans/[id]/actions.ts` `archivePlan`·`softDeletePlan`). 메인 리스트/인사이트/시나리오는 `.is(archived_at,null)`로 archived 숨김
- **B9 adhoc 시나리오**: `scenario-author-modal.tsx` 3모드(plan={plan} / adhoc={securities,initialTicker} / adhoc+lockTicker[종목상세]) + `(shell)/scenarios/actions.ts` `addSecurityScenario`(plan_id null·ticker·user_id). 모니터(`scenarios-screen.tsx`)에 adhoc 행 병합(`AdhocScenario` 타입, "새 시나리오" 버튼 배선) + 종목상세(`security-detail.tsx`)에 adhoc 시나리오 표시+"+시나리오추가"(secScenarios prop, page fetch)
- **B8 플랜 생성**: `components/plan/compose-modal.tsx`(단일모달, securities/portfolios 자체 페치, 이름+종목검색+메모+상태/포트폴리오/**전략(execId)** + "연속 생성") + `(shell)/plans/actions.ts` `createPlan`(human_id 자동·auto 시나리오 가격배수·auto 규칙)·`createPortfolio`. 3진입점: 사이드바 새플랜(app-shell `onNewPlan`)·플랜리스트 헤더·종목상세 "이 종목으로". **⚠️ 웹 적응: 실 종목만(FK), 미등록 티커 mock 분기 제거**
- **⚠️ 전략 드롭다운 버그**: 프로토타입 ComposeModal이 `STRATEGIES`(관점) 항목을 `t.strategy`("전략") 라벨로 붙인 mislabel — 이식 시 그대로 옴. **의도적 수정(프로토타입과 divergence): `EXEC_STRATEGIES`(전략)/`execId`로 교체** — 라벨+상세헤더(execId 관리)와 일관. `t.strategy`=전략(EXEC), `t.framework`=관점(STRATEGIES)
- **규칙 자동화 v1 (스텝1-3)**: 설계 상세는 개인 메모리 `rules-automation-design`. `20260705000100_rule_flags.sql`(rules에 `is_auto`/`edited`/`source`) + `lib/rules-from-strategy.ts`(웹인라인·net-new·골든무관: `rulesFromStrategy`[loc_pct→loc_hit매수, tp_pct→return익절]·`rulesFromGoal`·`autoRulesFor`, 파라메트릭%저장) + `plans/[id]/actions.ts` `regenerateAutoRules`((is_auto&&!edited)만 교체→커스텀/편집분 보존). 배선: createPlan·전략변경(patchPlanAction execId)·목표설정(setGoalAction). plan-mapper는 `UIRule`(isAuto/ruleSource) 확장 + rules select에 플래그 3컬럼. **v1스코프=무한매수법+목표**(그리드/VR은 절대밴드 선설정 필요→v2). E2E검증: 무한매수법→LOC+익절 자동, 목표→규칙, 전략없음↔무한매수법 재생성 안전(중복0, 편집분 보존). **미완=스텝4(규칙 편집/삭제/추가 UI, "규칙 추가" disabled 해소) + v2(밸류애버리징·정액분할·6:4·모멘텀 = 시간/비중/트레일링 트리거)**. 발동=알림(실제 발동엔진은 마일스톤6)

### 2026-07-05 3차 세션 이음새 (Phase C 실데이터 전환)
- **OHLCV 백필 파이프라인**: `apps/server/src/adapters/tiingo.ts`(신규, US 일봉 `fetchTiingoDaily` — 티커당 1콜 날짜범위, **원가격** upsert. adjClose는 응답에 있으나 미사용) + `kis.ts`에 `fetchKisDaily` 추가(KR 일봉 `inquire-daily-itemchartprice`/tr_id `FHKST03010100`, **호출당 최대 100봉 → 100캘린더일 윈도우로 과거 페이지네이션**·중복제거·오름차순) + `sync-ohlc.ts` CLI(`--market/--tickers/--years`, `security_price_history` upsert source='tiingo'|'kis', 500행 청크) + `db.ts` 공통 `PriceBar`/`PriceHistoryRow` + `env.tiingoApiKey`. **14/14 5년 백필 17,339봉 검증.** 실행: `pnpm --filter @keystone/server sync:ohlc -- --years 5`
- **⚠️ PostgREST max_rows=1000 무음 절단(중요)**: `/api/ohlc`가 5년 일봉(~1254행)을 오름차순 select 시 **가장 오래된 1000행만** 반환(supabase 로컬 config `max_rows` 기본 1000) → 최근 ~1년 조용히 사라짐. **스켈레톤(빈 테이블 bars=[]) 땐 안 드러나고 실데이터 유입으로 표면화.** 해법 = 라우트/로더에서 `.range(from, from+PAGE-1)` **페이지네이션**(page<PAGE면 종료). 전 종가 로더(`price-closes-query`·`/api/ohlc`·annual)에 적용
- **trajectory 시장가 seam 교체**: `planTrajectory(p, closes?)` 선택 인자 — closes 있으면 각 샘플의 월인덱스(Sep~Jun)를 실 달력 날짜로 되돌려(`monthIdxToISO`) 그 시점 종가 forward-fill(`closeAt` 이진탐색). 없으면 seededWalk mock 폴백. `isMockPath` 플래그(mock 배지 조건부). `lib/price-history-map.ts`(server-only 래퍼 `loadPriceCloses`/`attachPriceCloses`) + `UIPlan.priceCloses`. 배선: plans/[id]·plans 리스트에서 종가 첨부 → Sparkline(리스트/대시보드/상세, `closes` prop)·PerfBand·timeline-view 전 표면. **축(Sep~Jun 창)은 유지, mkt만 실 종가**(날짜앵커 실제화는 별도)
- **⚠️ server-only 모듈이 클라이언트 번들 오염(중요·Next RSC)**: `securities-list.ts`가 **클라이언트 컴포넌트(search-modal)에서 import**되는데 여기에 `import "server-only"`(+`supabaseServer`=next/headers) 모듈(`price-history-map`)을 물리면 **클라이언트 번들 오염 → 앱 전체 500**. 실데이터가 들어오자(genSpark seam) 표면화. 해법 = 쿼리를 **클라이언트-세이프 모듈로 분리**(`lib/price-closes-query.ts` `fetchClosesWith(supabase, tickers)` — 전달된 클라이언트 사용, server-only 미의존). server-only 래퍼(price-history-map)는 이걸 supabaseServer로 감싸는 얇은 래퍼. **교훈: client 컴포넌트에서 import되는 lib는 server-only 전이의존 금지 — 클라이언트 인자 버전으로 분리**
- **genSpark/mockChange seam 교체**: `mapSecurity(…, closes?)` — 실 종가 2봉+이면 spark=최근 40봉 종가·change=마지막 2봉 델타(%), 없으면 mock 폴백. 로더 2곳(`securities-list.ts` fetchAll/Watched·`security-detail-data.ts`)에서 `fetchClosesWith` 주입 → 관심종목/리서치/스크리너/검색/종목상세 전 표면 실 spark·등락률
- **날짜 앵커 중앙화 = 단일 `lib/clock.ts`(중요 패턴)**: golden-protected core `KS_REF={y:2026,mo:6,d:26}`에서 파생하는 `refNow()`(=앱 canonical 오늘)·`REF_YEAR`. 웹에 드리프트하며 흩어진 `new Date(2026,5,17/22)`·`"Jun 8"`·`curYear=2026`·parse용 `2025`를 **전부 clock 파생으로 통일**(값 Jun 26으로 일치). 교체: trajectory·props-sidebar·journal-screen·security-detail·gap-tab(qlab/entryDate/fillMarks/eventTicks/nowDate·firstDate, **축앵커는 월초 day=1 보존**)·timeline-view(TODAY_T)·strategy-tab·rule-eval-v2·inbox.ts(ibxBucket). **실제 today(real-now) 전환은 core KS_REF+clock.ts만 바꾸면 되는 1지점으로 축소**(시드·trajectory 창까지 얽힌 별도 대작업). ⚠️ **KS_REF는 core(골든)라 수정 불가 — 웹 중앙화만**
- **규칙 발동 워커**: `lib/rule-worker.ts`(server-only) `syncRuleFirings(supabase, userId, plans)` — 인박스 로드 시(서버) 활성 규칙을 `evalRuleV2`로 평가, **fired && last==="Never"(중복방지 게이트)** 규칙 → ① `rules.last_fired=refNow`(→ 기존 `buildInboxNotes`가 자동 표시, 인박스 재작성 없음) ② `notifications` insert(user 소유·kind=rule_fired·title=규칙명 L10n·body={en,ko} 발동사유·payload=스냅샷). `plans`의 `rule.last`도 인메모리 갱신(이번 렌더 즉시 노출). **멱등**(재로드 시 중복 insert 0). 시세 정적이라 로드 시 호출로 충분. 인박스 트리아지는 여전히 localStorage(DB 트리아지 전환=옵션2, 별도 대작업). E2E: 7규칙 발동→notifications 7행·인박스 "오늘" 버킷 표시·멱등 검증
- **fin-history seam 교체(밴드차트 연간주가)**: `finPriceHistory(plan, fin)` — `plan.annualCloses`(YYYY→종가, 상세페이지에서 `fetchAnnualCloses` 로드) 있으면 **위치 기반 정렬**(fin.is[i]→연도 `REF_YEAR-(n-1-i)`, 마지막=라이브 현재가). **`FinYearIS.y` 라벨 파싱 없이 위치+REF_YEAR로 정렬 → DB/seed fin 모두 안전**. 없으면 PX_TUNE mock 폴백. `fetchAnnualCloses`=일봉을 연도별 마지막 종가로 축약(페이지네이션)
- **~~⚠️ 남은 mock seam은 데이터 블로커~~ → 오판, 4차에서 해소**: gap iv·screener perLo/perHi가 "과거 재무 백필 필요"라 했으나, **실측하니 security_financials에 2021-2025 실 재무(dart/edgar)가 이미 있었음** — 백필 아니라 seam 교체였음(아래 4차 이음새).

### 2026-07-05 4차 세션 이음새 (live 전환 + 마지막 seam + 백로그 완전소진)
- **날짜앵커 real-now 전환(웹레이어만, 골든 무손상 — 핵심 패턴)**: `clock.ts` `refNow()`를 실 `new Date()`(로컬 자정)로, `REF_YEAR`=실 연도로. **함정: golden-protected core `inferYear`가 KS_REF=6/26 경계를 못박음**(goldens.json이 `fmtDate("Jun 27")="Jun 27, 2025"` 하드코딩) → refNow만 바꾸면 7월이 전년(2025) 추론. **해법 = `inferYearWeb(mo,d)`가 core inferYear 규칙 복제**(웹 신규 파싱용, core 무수정) + year-less 파싱 통일(gap-tab/strategy-tab/rule-eval-v2/props-sidebar의 `REF_YEAR-1` 하드코딩 교체). **trajectory 함정: 고정 Sep~Jun 창이라 7월 "Jul"이 창밖→`todayT`=NaN→차트 붕괴** → **rolling 10개월 창**(refNow 월로 끝, `TRAJ_MONTHS` 단일 소스 export, timeline-view/perf-band 소비, `trajSlotYear`로 툴팁 실 연도)으로 재설계. price-closes-query `CLOSES_WINDOW_START`도 rolling 파생. 골든 102 유지·real-now 완전 전환은 이제 clock.ts 1지점
- **실시간 WS 스트리밍(Supabase Realtime 재활용 — 핵심 아키텍처)**: 커스텀 SSE 안 만들고 **worker→DB→Supabase Realtime→client** 파이프. `apps/server`: `kis.ts` `kisApprovalKey()`(oauth2/Approval, `secretkey` 필드) + `kis-ws.ts`(H0STCNT0 실시간 체결가, WS `ws://ops.koreainvestment.com:21000`, 구독 tr_type "1", **PINGPONG echo 필수**, 파이프 프레임 `현재가=[2]·등락률=[5]`) + `finnhub-ws.ts`(`wss://ws.finnhub.io`, trade `{s,p}`, changePct null) + `stream-quotes.ts` worker(KR→KIS·US→Finnhub, 1.5초 배치 upsert, 재연결). 마이그레이션 `live_quotes`(ticker PK, price/change_pct/ts) + **`supabase_realtime` publication 등록**. 웹: `live-quotes-provider.tsx`(client, `supabaseBrowser().channel().on("postgres_changes")` 구독+초기페치, `useLiveQuote(ticker)`) app-shell 마운트 → list-view/detail-view/watchlist/security-detail이 `currentPrice` override(+`live-dot`). ⚠️ **실 틱은 장중(평일)에만** — 시뮬레이션 틱(수동 live_quotes upsert)으로 DB→client 파이프 E2E 증명. 실행 `pnpm --filter @keystone/server stream:quotes`
- **마지막 mock seam = "백필 아니라 seam 교체"(교훈: DB 상태 실측부터)**: gap iv·screener perLo/perHi가 과거 재무 필요했는데 **security_financials에 2021-2025 실 재무 이미 있었음**. `valuation-history.ts`(신규, 공유): **EPS_year = `fin.is[i].net/(sharesOut×1e6)`**(core buildFinFromSeed가 shares×1e6), **PE_year = 연말종가/EPS_year**, `peBand`=min/max, `fairMultiple`=√(perLo×perHi), IV=최신EPS×fairMultiple. screener는 SEC_SEED→실측(buildPlanFin+annual closes 배치로드, SEC_SEED 폴백). gap-history는 TUNE mock→실 iv/ivHistory/priceHistory(GapTab 구조 호환 유지). **실데이터가 mock보다 정확: 삼성은 mock이 항상 "저평가"였는데 실데이터는 주가 3배 상승 후 "고평가" 판정**
- **인박스 트리아지 DB 이전(옵션2)**: read/resolved/muted가 localStorage→DB. **인박스 노트는 plans/rules 파생**(복합키 `{planId}:{ruleId}` / `{planId}:scn:{id}`), notifications(rule-fired UUID)와 1:1 아니고 시나리오 알림은 notification 행 없음 → **notifications.read_at 대신 복합노트키 전용 `inbox_triage` 테이블**(PK user_id,note_key; read/resolved/muted_at; 사용자소유 RLS). inbox page가 로드→초기 Set props, `inbox/actions.ts` `setTriage`/`markAllRead` 서버액션(낙관적 UI+undo 유지). `lib/inbox-triage.ts`(localStorage) 제거. 리로드=fresh 서버=다른 기기 → 영속 증명(안읽음 25→24). 사이드바 unread 뱃지는 레이어 비용으로 defer(`inboxUnread` prop 준비됨)
- **adhoc 시나리오 CRUD**: 종목단독 시나리오가 add만 되던 것 → `scenarios/actions.ts` `updateSecurityScenario`/`deleteSecurityScenario`(플랜 패턴 미러, planId 없이 scenarioId, RLS user_id 소유권). `AdhocScenario`/`SecScenario`에 `dbId`/`caseT` 추가+매퍼 채움(select에 id·case_t). `scenario-author-modal` adhoc 편집 분기. **adhoc 행에만** 수정/삭제 버튼(플랜 시나리오 제외). 스키마 변경 없음(scenarios가 이미 adhoc 지원)
